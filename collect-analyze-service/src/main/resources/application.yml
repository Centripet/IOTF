server:
  port: 46551

spring:
  application:
    name: collect-analyze-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml

  config:
    import: nacos:collect-analyze-service.yaml

management:
  endpoints:
    web:
      exposure:
        include: '*'

emqx:
  url: tcp://localhost:41883
  client-id: collect-analyze-service
  topics: energy/device/+/data
  qos: 1
  clean-session: false